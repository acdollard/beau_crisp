---
import "../styles/global.css";
import { Image } from "astro:assets";
import logo from "../assets/buried1.png";
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="description"
      content="Beau Crisp - Strength & Conditioning Coach"
    />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Bebas+Neue:wght@300;400&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>
  </head>
  <body class="min-h-screen">
    <header
      class="bg-stone-200 fixed top-0 left-0 right-0 z-50 shadow font-customFont text-2xl tracking-wide"
    >
      <nav class="container mx-auto px-4 py-6">
        <div class="flex justify-between items-center">
          <a href="/" class="text-xl font-bold text-stone-300"
            ><Image src={logo} alt="Beau Crisp Logo" width={150} /></a
          >

          <!-- Mobile menu button -->
          <button
            id="mobile-menu-button"
            class="md:hidden text-slate-800 transition-transform duration-200"
            aria-label="Toggle menu"
          >
            <svg
              class="w-12 h-12"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
          </button>

          <!-- Desktop menu -->
          <div class="hidden md:flex space-x-6">
            <a
              href="/"
              class="text-zinc-800 hover:text-rose-600 transition-colors duration-200 text-3xl"
              >Home</a
            >
            <a
              href="/blog"
              class="text-zinc-800 hover:text-rose-600 transition-colors duration-200 text-3xl"
              >Blog</a
            >
            <a
              href="/workouts"
              class="text-zinc-800 hover:text-rose-600 transition-colors duration-200 text-3xl"
              >Workouts</a
            >
            <a
              href="/about"
              class="text-zinc-800 hover:text-rose-600 transition-colors duration-200 text-3xl"
              >About</a
            >
            <a
              href="/login"
              class="text-zinc-800 hover:text-rose-600 transition-colors duration-200 text-3xl"
              >Login</a
            >
          </div>
        </div>

        <!-- Mobile menu -->
        <div
          id="mobile-menu"
          class="hidden md:hidden mt-4 space-y-4 transform transition-all duration-300 ease-in-out"
        >
          <a
            href="/"
            class="block text-zinc-800 hover:text-rose-600 transition-colors duration-200"
            >Home</a
          >
          <a
            href="/blog"
            class="block text-zinc-800 hover:text-rose-600 transition-colors duration-200"
            >Blog</a
          >
          <a
            href="/workouts"
            class="block text-zinc-800 hover:text-rose-600 transition-colors duration-200"
            >Workouts</a
          >
          <a
            href="/about"
            class="block text-zinc-800 hover:text-rose-600 transition-colors duration-200"
            >About</a
          >
          <a
            href="/login"
            class="block text-zinc-800 hover:text-rose-600 transition-colors duration-200"
            >Login</a
          >
        </div>
      </nav>
    </header>
    <main class="mx-auto">
      <slot />
    </main>
    <footer class="bg-stone-200 mt-auto">
      <div class="container mx-auto px-4 py-6 text-center">
        <p>
          &copy; {new Date().getFullYear()} Beau Crisp. All rights reserved.
        </p>
      </div>
    </footer>
  </body>
</html>

<script>
  // Mobile menu toggle with animation
  const mobileMenuButton = document.getElementById("mobile-menu-button");
  const mobileMenu = document.getElementById("mobile-menu");
  const menuIcon = mobileMenuButton?.querySelector("svg");

  mobileMenuButton?.addEventListener("click", () => {
    mobileMenu?.classList.toggle("hidden");
    mobileMenu?.classList.toggle("opacity-0");
    mobileMenu?.classList.toggle("translate-y-[-10px]");

    // Animate the hamburger icon
    menuIcon?.classList.toggle("rotate-90");
  });

  // Initialize menu state
  if (mobileMenu) {
    mobileMenu.classList.add("opacity-0", "translate-y-[-10px]");
  }
</script>

<style>
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
  }

  /* Add smooth transitions */
  #mobile-menu {
    transition: all 0.3s ease-in-out;
  }

  #mobile-menu:not(.hidden) {
    opacity: 1;
    transform: translateY(0);
  }

  #mobile-menu.hidden {
    opacity: 0;
    transform: translateY(-10px);
  }
</style>
